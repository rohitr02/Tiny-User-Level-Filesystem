# Tiny-User-Level-Filesystem
Creating a User Level Filesystem with the FUSE Library in C

This project is not fully working. Every method except for the reading and writing into the user filesystem works.
